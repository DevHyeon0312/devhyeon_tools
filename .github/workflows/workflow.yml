name: Flutter Web
on:
  push:
    branches:
      - master
jobs:
  build:
    name: Build Web
    env:
      my_secret: $
    steps:
      - uses: actions/checkout@v3
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.4'
          channel: 'stable'
      - run: flutter --version
      - run: flutter config --enable-web
      - run: flutter pub get
      - run: flutter build web --release
        working-directory: ./Users/devhyeon_github/devhyeon_tools
      - run: |
          # 2. change path to [existed lib/main.dart path]/build/web
          cd ./Users/devhyeon_github/devhyeon_tools/build/web
          git init
           # 3. change your email
          git config --global user.email devhyeon0312@gmail.com
          # 4. change your name
          git config --global user.name DevHyeon0312
          git status
          # 5. change your repository
          git remote add origin https://$@github.com/DevHyeon0312/devhyeon_tools.git 
          git checkout -b gh-pages
          git add --all
          git commit -m "update"
          git push origin gh-pages -f